# 합의_0612 — Notion/Figma/docs 기능·화면 ID 통합

## 합의 일자

- 2026-06-12

## 기준 원본

- Notion 페이지: `기능 명세서`
- Page ID: `3651d8db-b5f5-80b2-b15a-f1d62ce1acfe`
- 원칙: Notion의 기능 ID와 화면 ID를 canonical 기준으로 삼고, 로컬 문서와 Figma를 이 기준에 맞춘다.

## 합의 내용

1. Figma 프레임명은 Notion 기준 화면 ID로 통일한다.
2. 하나의 화면에 여러 기능이 연결될 수 있으므로 화면 ID와 기능 ID를 분리한다.
3. Figma의 `화면설명(Description)` 칸에 `관련 기능 ID`를 추가한다.
4. 상태/모달 프레임은 `기준 화면 ID / 상태명` 개념으로 관리한다.
5. Notion에 없던 Figma 공통 상태는 `COMMON-*` 기능 ID로 최소 보강한다.

## 추가/보강한 기준

| 항목 | 결정 |
|---|---|
| 원본 자료 조회 팝업 | `SRC-POP-01`의 기준 화면을 `STD001 / 원본 자료 조회 팝업`으로 보강 |
| 빈 상태 | `COMMON-EMPTY-01`로 보강 |
| 로딩/실패/토스트/스피너/에러 | `COMMON-FDB-01`로 보강 |
| MVP 제외 안내 | `COMMON-OUT-01`로 보강 |
| 권한/비활성 정책 | `COMMON-AUTH-01`로 보강 |
| 로그아웃 | Notion 기존 기능 `AUTH-OUT-01`을 `COMMON / 공통 프로필 메뉴`에 매핑 |

## 산출물 반영 위치

- Notion: `기능 명세서` 하위 `📎 5. 원본 자료 조회 팝업`, `🌐 7. 공통/글로벌`
- 로컬 문서: `docs/화면설계서.md`, `docs/wire_frame_requirements/00-index.md`
- Figma: `Stology_PM_Wireframe` 프레임명, `화면 ID`, `화면설명(Description)`
